import { BaseInputParams, InputBindingPlugin } from '@tweakpane/core';
interface RadioGridInputParams<T> extends BaseInputParams {
    cells: (x: number, y: number) => {
        title: string;
        value: T;
    };
    groupName: string;
    size: [number, number];
    view: 'radiogrid';
}
export declare const RadioGridNumberInputPlugin: InputBindingPlugin<number, number, RadioGridInputParams<number>>;
export {};
